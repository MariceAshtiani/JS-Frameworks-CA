import styled from "styled-components";

export default styled.div`
    position: relative;
    cursor: pointer;

    span {
        position: absolute;
        font-size: 1rem;
        color: orange;
        padding: 0 5px;
        left: 20px;
    }
`;